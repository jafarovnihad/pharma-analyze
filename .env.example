# Data source: "mysql" (local test DB) or "api" (company JSON web service)
DATA_SOURCE=mysql

# Local test DB (used when DATA_SOURCE=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rx_analyze
# DB_SSL=true

# Company web service (used when DATA_SOURCE=api)
COMPANY_API_URL=http://localhost:8080/stock
COMPANY_API_KEY=

PORT=3000
